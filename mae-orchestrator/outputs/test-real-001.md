It seems write permission hasn't been granted yet. Please approve the write permission when prompted, or let me know if you'd like me to try a different approach.

The file will contain:
- `parse_args()` with `argparse` — optional `--name` flag (defaults to `"MAE"`)
- `greet(name: str) -> str` with full type annotations
- `main() -> None` entry point that prints `Hello MAE!` by default
